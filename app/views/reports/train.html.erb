<h1>Reports#train</h1>
<p>Find me in app/views/reports/train.html.erb</p>
<%= @rowarraydisp %>

<table border="0" cellspacing="5" cellpadding="5" class = "table">
  <tr>
  <% @csv_table.headers.drop(1).each do |header| %>
    <th><%= header %></th>
  <% end %> 
  </tr>
  <% @csv_table.each do |row| %>
    <tr>
    <% row.drop(1).each do |element| %>
      <td><%= element[1] %></td>
    <% end %>
    </tr>
  <% end %> 
</table>

<%= image_tag 'rf.png' %>

